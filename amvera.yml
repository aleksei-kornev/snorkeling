meta:
  environment: jvm
  toolchain:
    name: maven
    version: 11
build:
  artifacts:
    target/*.jar: /
  args: 'spring-boot:repackage '
run:
  jarName: snork.jar
  persistenceMount: /data
  containerPort: 80
